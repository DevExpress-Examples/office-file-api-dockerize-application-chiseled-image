# office-file-api-dockerize-application-chiseled-image
Dockerize an .NET Console application in a chiseled docker image. The application uses the Office File API library to convert Word documents to PDF. 
